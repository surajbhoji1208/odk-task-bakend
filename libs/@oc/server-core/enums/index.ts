export enum UserStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
    SUSPENDED = 'suspended',
}

export enum UserType {
    ADMIN = 'admin',
    STANDARD = 'standard',
}

export enum FieldTypeEnum {
    String = 'String',
    Number = 'Number',
    UUID = 'UUID',
    Date = 'Date',
    Boolean = 'Boolean',
    Array = 'Array',
}